<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Create Account - PHHB</title>
    <link rel="stylesheet" type="text/css" href="/css/createAccount.css">
  </head>
  <body>
    <% include layout %>
    <main>
      <form action="/PHH_Bookmark/createAccount" method="post">
        <h2>Create an Account</h2>
        <label><p class="read">Email Address</p><input name="email" type="text"><p class="readInput">メールアドレスを入力してください</p></label>
        <% if (typeof eMailExists !== 'undefined') { %>
          <p><%= eMailExists %></p>
        <% } %>
        <% if (typeof mailNotice !== 'undefined') { %>
          <p class="error"><%= mailNotice %></p>
        <% } %>
        <label><p class="read">Username</p><input name="user_name" type="text"><p class="readInput">ユーザーネームを入力してください</p></label>
        <% if(typeof userNameExists !== 'undefined'){ %>
          <p><%= userNameExists %></p>
        <% } %>
        <% if (typeof usernameNotice !== 'undefined') { %>
          <p class="error"><%= usernameNotice %></p>
        <% } %>
        <label><p class="read">Password</p><input name="password" type="password"><p class="readInput">パスワードを入力してください</p></label>
        <label><p class="read">Again Password</p><input name="again_password" type="password"><p class="readInput">もう一度パスワードを入力してください</p></label>
        <% if (typeof passwordNotice !== 'undefined') { %>
          <p class="error"><%= passwordNotice %></p>
        <% } %>
        <input id="submitBtn" type="submit" value="Create an account" class="button">
      </form>
    </main>
    <!-- <footer>
      <a class="site-footer-vantan" href="http://www.vantan-hs.com/index.php">
        <img src="img/vantan_logo.png">
      </a>
      <a class="site-footer-phh" href="http://www.vantan-hs.com/phh/index.php">
        <img src="img/phh_logo.png">
      </a>
      <a class="site-footer-n-highschool" href="https://nnn.ed.jp/">
        <img src="img/n_highschool_logo.png">
      </a>
    </footer> -->
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
    <script>
      $(function(){
        var $email = $('[name="email"]');
        var $userName = $('[name="user_name"]');
        var $button = $(".button");
        var $password = $('[name="password"]');
        var $againPassword = $('[name="again_password"]');
        var checkForm = /^[a-zA-Z0-9]+$/;
        var checkEmail = /^([a-zA-Z0-9])+([a-zA-Z0-9\._-])*@([a-zA-Z0-9_-])+([a-zA-Z0-9\._-]+)+$/;

        $button.click(function(){
          this.disabled = true;
          this.form.submit();
        });
        $email.keyup(function(){
          var email = $email.val();
          $('#emailNotice').detach();
          if(!checkEmail.test(email)) $email.after('<p id="emailNotice">正しいメールアドレスを入力してください</p>');
        });
        $userName.keyup(function(){
          var userName = $userName.val();
          $('#userNameNotice').detach();
          if(!checkForm.test(userName) || userName.length > 16) $userName.after('<p id="userNameNotice">ユーザーネームは半角英数16文字以下です');
        });
        $password.keyup(function(){
          var password = $password.val();
          $('#passwordNotice').detach();
          if(!checkForm.test(password) || password.length < 8) $password.after('<p id="passwordNotice">パスワードは半角英数８文字以上です');
        });
        $againPassword.keyup(function(){
          var againPassword = $againPassword.val();
          $('#againPasswordNotice').detach();
          if(!checkForm.test(againPassword) || againPassword.length < 8) $againPassword.after('<p id="againPasswordNotice">パスワードは半角英数８文字以上です');
        });
      });
    </script>
  </body>
</html>
